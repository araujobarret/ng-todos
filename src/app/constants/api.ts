const API_URL = 'https://todos-araujobarret.herokuapp.com';

export const USERS_LOGIN = `${API_URL}/users/login`;
export const USERS_ME = `${API_URL}/users/me`;

export const TODOS_GET = `${API_URL}/todos`;
